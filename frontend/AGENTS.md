# Frontend instructions

在执行任何 `frontend` 目录下的任务之前，必须完整阅读：

- `FRONTEND_REQUIREMENTS.md`
- `FRONTEND_CONSTRAINTS.md`

前端功能、页面范围和验收标准以
`FRONTEND_REQUIREMENTS.md` 为准。

前端技术栈、目录结构、编码规范和实现限制以
`FRONTEND_CONSTRAINTS.md` 为准。

修改完成后，检查实现是否同时符合这两个文件。