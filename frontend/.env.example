# The real Go API is the default. Set true only for isolated UI development.
VITE_USE_MOCKS=false

# Leave blank for same-origin requests. Do not add production endpoints here.
VITE_API_BASE_URL=/api/v1
